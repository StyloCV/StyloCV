# cv_model

Generate cv using typest and pydantic models.

Functional capabilities:

- json -> pydantic model validation
- pydantic model + typst template -> typst script
- typst script -> pdf (via wasm in browser or typst cli)
